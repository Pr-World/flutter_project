import 'package:flutter/material.dart';

class MyPage {
  static dynamic home() {
    return Scaffold(
      appBar: AppBar(title: const Text("Hello World!")),
      body: const Center(child: Text("Plz work :)")),
    );
  }
}
